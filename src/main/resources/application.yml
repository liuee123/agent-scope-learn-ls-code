# ==========================================
# 第 1 节：课程导览与环境搭建
# 基础配置 — 仅设置服务端口
# ==========================================

server:
  port: 8080

spring:
  main:
    banner-mode: off
  application:
    name: section-01-hello-agentscope

# AgentScope 配置（第 2 节开始使用，此处预留）
# agentscope:
#   model:
#     deepseek:
#       type: openai
#       base-url: https://api.deepseek.com
#       api-key: ${DEEPSEEK_API_KEY}
#     dashscope:
#       type: dashscope
#       api-key: ${DASHSCOPE_API_KEY}
