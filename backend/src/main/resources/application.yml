# ==========================================
# AgentScope 课程 — 应用配置
# ==========================================

server:
  port: 8080

spring:
  main:
    banner-mode: off
  application:
    name: agentscope-course

# ==========================================
# AgentScope AG-UI Starter 配置
# ==========================================
agentscope:
  agui:
    path-prefix: /agui
    enable-reasoning: true
